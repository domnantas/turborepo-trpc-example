{
  "name": "turborepo-trpc-example",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "db:push": "turbo db:push"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {}
}
